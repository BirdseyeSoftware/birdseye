(ns birdseye.test.url-mapper
  (:use [clojure.test])

  (:require [clojure.pprint :as pp]
            [clojure.repl :as repl])
  (:use [birdseye.url-mapper]))

(deftest replace-me ;; FIXME: write
  (is false "No tests have been written."))
